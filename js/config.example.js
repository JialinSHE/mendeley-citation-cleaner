// Copy this file to `config.js` and fill in your own values.
// (config.js is git-ignored so your details are never committed.)

// Register an app at https://dev.mendeley.com/myapps.html to get a Client ID.
// Redirect URL must exactly match where callback.html is served from
// (e.g. https://<you>.github.io/mendeley-citation-cleaner/callback.html,
// or http://localhost:8000/callback.html for local testing).
export const MENDELEY_CLIENT_ID = "";
export const REDIRECT_URI = "";

// Your email — sent with CrossRef requests so they land in CrossRef's faster
// "polite pool". Optional but recommended.
export const CROSSREF_MAILTO = "";
